# Database connection variables for Grad Cafe Analytics (Step 3).
# Copy this file to .env and fill in your real values. Do not commit .env.
#
# Variable names match PostgreSQL libpq convention (PGHOST, PGPORT, etc.).

PGHOST=localhost
PGPORT=5432
PGDATABASE=your_database_name
PGUSER=your_db_username
PGPASSWORD=your_db_password
